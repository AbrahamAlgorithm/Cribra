server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router client-side routing — any path not matching a real file
    # falls back to index.html, letting the app's own router handle it.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
